[build]
  command = "npm run build"
  publish = "dist"

# Astro's Netlify adapter generates the serverless functions and edge config.
# Netlify Blobs is wired in automatically for this site's functions — no connection string.
